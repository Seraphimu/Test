### 信号量机制

有空地: empty

有数据: full

先加锁, 再检测, 本质也是访问临界资源.

信号量: 在检测之前就能得知资源数?

环形队列并发, 只有空和满会相遇.

谁先运行不确定, 但谁先申请到资源是确定的.

### 线程池

来一个任务, 创建一个线程. 创建线程有成本. 多个线程, 要不要被管理起来呢? 怎么管理? 先描述, 再组织. 

预先存储起来, 存粮. 池化技术: 屯粮.

原来学习多线程的目的就是处理任务, 而不是单纯为了学会它. 线程技术 + 每个线程里塞上算法 == 多个线程解决问题. 原来学习系统是学习网络的基础. 

